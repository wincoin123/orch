server {
    listen 80;
    {% for ip in ip_list %}
    deny {{ ip }};
    {% endfor %}
    real_ip_header X-Forwarded-For;
    location / {
        proxy_pass {{ server_ip }};
        root /usr/share/nginx/html;
    }
}
